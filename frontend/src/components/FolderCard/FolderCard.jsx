import './FolderCard.css';

function FolderCard(props) {

    const folderName = "";

    return (
        <div className = "folder-card"><p>{folderName}</p></div>
    );
}

export default FolderCard;